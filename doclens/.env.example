# Mistral API key. Server-side only — never expose this to the browser.
# Get one at https://console.mistral.ai/
MISTRAL_API_KEY=

# Model overrides (optional). Defaults are set in src/lib/env.ts.
# MISTRAL_OCR_MODEL=mistral-ocr-latest
